阶段：第3阶段进展
日期：2026-05-17

一、实现内容
- 新增记忆对话脚本：`src/phase3_memory_chat.py`。
- 使用 `RunnableWithMessageHistory` 实现短期记忆。
- 使用 `session_id` 实现会话隔离，不同会话互不影响。

二、测试结果
- `session=s1`：先说“我叫小明”，再问“我叫什么？”，可正确回答“小明”。
- `session=s2`：直接问“我叫什么？”，不会读取 `s1` 的记忆。

三、下一步
- 进入第4阶段：实现长期偏好记忆（JSON持久化）与对应工具。
